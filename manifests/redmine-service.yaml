apiVersion: v1
kind: Service
metadata:
  name: redmine
  namespace: pmotrack
spec:
  type: NodePort
  selector:
    app: redmine
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080
